# RVC 视频伴侣 — 未完成事项交接文档

**交接日期**：2026-08-02 06:05（洁癖收尾刷新） **交接人**：甄宇航（Zhen）· 工程保障团队主理人 **接手人**：下一个会话窗口 **前置阅读**：`.workbuddy/memory/MEMORY.md`（项目长期记忆，含坑位速查）

---

## 一句话现状

**✅ v3.2.3 全套发布完成**（2026-08-02 晚间）：main = `d453b12` 已推送 origin/main，Release v3.2.3 正式发布（含 RVC-Video-Companion.zip），CI success。MP3 音频播放 + 热键两修复 + 文档/记忆同步全部收尾。

---

## 2026-08-02 晚间更新（v3.2.3 已完成）

- 本地 main 新增 4 提交（b29531b MP3 音频 / 6e7a01e playPending 过渡锁 / d13b711 双通道去重 / d453b12 版本+文档），全部验证全绿（L0 14/14 + L1 37/37 + L2 8/8 + QA 行为/时序验证 NoOne；pre-push 12/12；CI success）。
- 事项 1 已完成：用户终端跑 `bash packaging/release.sh` → build .app + distro zip + gh release，v3.2.3 正式发布（draft→edit --draft=false），资产 RVC-Video-Companion.zip（26.5MB）。
- 打包版实测：health version=3.2.3 + ffmpeg_source=builtin + mp3 请求 Content-Type=audio/mpeg。
- 遗留（低优先）：B13 选目录前置待领导亲验；Range 416 建议后续修；play() 无超时兜底。

---

## 当前仓库状态（实测 2026-08-02）

| 项              | 值                                         |
| -------------- | ----------------------------------------- |
| 本地 main        | `fc0e50b`                                 |
| 远端 origin/main | `fc0e50b`（同步）                            |
| 版本            | 3.2.2（manifest.json 唯一源）                |
| 静态检查         | `bash scripts/check.sh --static` 14/14 通过 |
| 完整验收         | L0 14/14 + L1 29/29 + L2 5/5 + L3 12/12 全绿（pre-push 自动跑） |

**近 4 个 commit**（从旧到新）：

```
0152371  fix(ux+transcode): 文件选择入口收敛 + 转码错误竞态修复（P0-P2）
6fbb3f9  fix(startup): Finder 双击 .command 场景补 brew PATH + 绝对路径调用
fc0e50b  feat(startup): 启动脚本增加 pynput 自检，热键依赖缺失不再静默
```

---

## 未完成事项（按顺序做）

### 事项 1：重打 .app + 发布新 Release【P0，唯一待办】

**背景**：dist 的 .app/zip 仍是 04:45 旧版（v3.2.2-test 打包），**不含** 0152371/6fbb3f9/fc0e50b 三个提交的改动（文件选择 UX + 转码竞态修复 + 启动脚本修复）。用户 Chrome 若加载 dist 目录扩展会看到旧行为。

**怎么做**（用户终端，沙箱拦构建所以必须本地跑）：

```bash
cd ~/ai-brain/浏览器播放器系统
bash packaging/release.sh
```

（build.sh 构建 .app + ad-hoc 签名 → make-distro.sh 打 zip → gh release upload。若想干净新 release 而非覆盖 v3.2.2-test，可考虑 manifest 版本号 3.2.2 已存在——v3.2.2 是 Latest、v3.2.2-test 是 Pre-release，本次建议覆盖 v3.2.2-test 或新建 tag，用户裁决。）

**验收**：

```bash
curl -s http://127.0.0.1:8765/api/health
# 期望：{"ok": true, "name": "RVC 视频伴侣", "version": "3.2.2", "ffmpeg": true, ...}
```

### 事项 2（可选，低优先）：行动清单剩余中/低项

- Developer ID 正式签名（$99/yr，用户当前接受 ad-hoc + 手动授权）
- P3 状态机简化（overlay 集中化，Archi ADR 单独立项，验收稳定后评估）

详见：`deliverables/engineering-assurance/full-review-incident-rvc-2026-08-02.md`（swift-thunder-newton 执行报告）。

---

## 关键文件索引

| 文件 | 用途 |
| --- | --- |
| `packaging/release.sh` | 一键发布（build→distro→gh release） |
| `.github/workflows/ci.yml` | push/PR main 触发 L0+L1+L2 |
| `stream-server/server.py` | 本地服务器（http.server，含 SSE 热键通道 + /api/stream-error 长轮询） |
| `reader-video-companion/content.js` | Chrome 扩展前端（1478 行） |
| `stream-server/packaging/build.sh` | .app 构建 + 签名 |
| `packaging/make-distro.sh` | 打发行 zip |
| `scripts/check.sh` | L0 静态检查（14 项） |
| `run_tests.sh` | 分层测试入口 |
| `deliverables/engineering-assurance/full-review-incident-rvc-2026-08-02.md` | swift-thunder-newton 执行报告 |

---

## 待用户确认的清理候选（未删，一个都没动）

| 对象 | 性质 | 建议 |
| --- | --- | --- |
| `截屏2026-08-01 18.06.10.png` | 未跟踪，会话期间截屏 | 确认无用可删 |
| `截屏2026-08-01 18.07.13.png` | 未跟踪，会话期间截屏 | 确认无用可删 |
| `deliverables/代码审核报告-2026-08-02.md` | 未跟踪交付物，与 engineering-assurance/full-review-incident-rvc-2026-08-02.md 内容重叠 | 二选一保留 |
| `.trae/specs/revert-playlist-frameless-scaling/` | 历史任务 spec 三件套，任务早已完成 | 确认过期可删 |

---

## 给接手会话的提醒

1. **先盘点再动手**：`git log --oneline -10` + `git status` + 读 `MEMORY.md`。别假设工作区状态。
2. **推送前务必处理代理**：这是过去几轮反复卡住的同一个坑（三态规则见 CLAUDE.md）。
3. **构建只在用户终端跑**：沙箱批量删除保护会拦 build.sh，别在 agent 里重试浪费时间。
4. **硬事实磁盘复核**：任何"文件不存在/接口没鉴权"类结论，先 ls/grep 实测再采信。
5. **验收环境两坑**：HTTP_PROXY 注入 + 服务器跨调用死亡（详见 CLAUDE.md「当前状态」），L3 必须同一命令内清代理 + 起服务器。
